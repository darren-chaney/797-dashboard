/* ============================================================
   mash-storage.js
   SAFE stub — no exports, no modules
   ============================================================ */

(function(){
  window.MASH_STORAGE = {
    load: function(){
      return null;
    },
    save: function(){
      return true;
    }
  };
})();
