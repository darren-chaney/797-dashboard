/* ============================================================
   mash-log.js
   SAFE stub — no exports, no modules
   ============================================================ */

(function(){
  window.MASH_LOG = {
    write: function(){
      // no-op
    }
  };
})();
