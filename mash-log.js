/* ============================================================
   mash-log.js
   SAFE stub — no exports, no modules
   ============================================================ */

(function(){
  window.MASH_LOG = {
    start: function(){ /* stub */ },
    add: function(){ /* stub */ }
  };
})();
